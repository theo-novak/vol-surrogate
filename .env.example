# Path to the DuckDB database file
DB_PATH=data/vol_surrogate.duckdb

# Path to the generated training dataset (.npz)
DATASET_PATH=data/heston_dataset.npz

# Directory for trained model checkpoints (committed demo weights live in models/)
MODELS_DIR=models
